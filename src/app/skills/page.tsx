import Skills from "@/components/Skills";
import PageShell from "@/components/PageShell";

export const metadata = {
  title: "Skills — Vishi Agarwal",
};

export default function SkillsPage() {
  return (
    <PageShell>
      <Skills />
    </PageShell>
  );
}
