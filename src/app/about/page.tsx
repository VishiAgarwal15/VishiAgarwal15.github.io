import About from "@/components/About";
import PageShell from "@/components/PageShell";

export const metadata = {
  title: "About — Vishi Agarwal",
};

export default function AboutPage() {
  return (
    <PageShell>
      <About />
    </PageShell>
  );
}
